python
import RPi.GPIO as GPIO, time

BUZZER_PIN = 26
GPIO.setmode(GPIO.BCM)
GPIO.setup(BUZZER_PIN, GPIO.OUT)

p = GPIO.PWM(BUZZER_PIN, 440)  # A4
p.start(50)
time.sleep(1.0)
p.ChangeFrequency(880)         # A5
time.sleep(1.0)
p.stop()
GPIO.cleanup()
